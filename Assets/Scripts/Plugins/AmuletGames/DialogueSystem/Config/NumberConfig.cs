namespace AG.DS
{
    public static class NumberConfig
    {
        /// <summary>
        /// The maximum value of delay second that can be set in the dialogue editor window.
        /// </summary>
        public const int MAX_DELAY_SECOND = 99999;


        /// <summary>
        /// The minimum value of delay second that can be set in the dialogue editor window.
        /// </summary>
        public const int MIN_DELAY_SECOND = 0;
    }
}